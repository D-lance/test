该版本基于webdriver.remote模式，即代码可以远程运行，如在A机器上运行代码，B机器浏览器运行

运行前需运行：java -jar selenium-server-standalone-3.13.0.jar

webdriver.remote配置项为config.config01.browser_config中，如果selenium-server在本地运行，则地址为127.0.0.1

config.login_config为登录配置项，输入准确的老白账号密码

python 3.4

只开启一次chrome浏览器

